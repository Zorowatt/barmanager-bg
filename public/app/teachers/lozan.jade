.modal-header
    button.close(ng-click="cancel()") &times;
    h3.modal-title {{teacher.name}}
.modal-body
    ul
        li(ng-repeat="t in teacher.concourse track by $index") {{t}}
        hr
    h4 {{teacher.additionalInfo}}
.modal-footer
    button.btn.btn-warning(ng-click="cancel()") Ok